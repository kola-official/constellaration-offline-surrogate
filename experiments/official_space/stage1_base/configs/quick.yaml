run_id: quick_seed0
mode: quick
seed: 0
audit_budget_per_method: 10
vmec_workers: 8
hardware:
  cpu_workers: 8
  dataloader_workers: 0
  torch_num_threads: 4
  gpu_devices: [0, 1]
  parallel_ensemble: true
data:
  label_mode: default_all
  auxiliary_label_min_finite_fraction: 0.98
  max_rows:
  optimization_validation_max_rows: 5000
  test_size: 0.15
  validation_size: 0.15
surrogate:
  regression_labels: auto
  problem2_label_weight: 1.0
  auxiliary_label_weight: 0.25
  label_weights:
    log10_qi: 2.0
  ensemble_members: 4
  width: 256
  blocks: 3
  dropout: 0.03
  learning_rate: 0.001
  weight_decay: 0.0001
  batch_size: 2048
  max_epochs: 80
  patience: 12
candidates:
  per_method_pool: 200
  relaxed_gmm_samples: 2000
  cmaes_budget: 3000
  batch_size: 4096
